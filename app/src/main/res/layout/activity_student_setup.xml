<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark">

    <!-- Header Section -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="Back"
            android:src="@android:drawable/ic_menu_revert"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/white" />

        <TextView
            android:id="@+id/header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="serif"
            android:text="@string/set_up_profile"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/step_1_2"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/setup_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginHorizontal="16dp"
        android:progress="50"
        android:progressDrawable="@drawable/custom_progress_bar"
        app:layout_constraintTop_toBottomOf="@id/header" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintBottom_toTopOf="@id/btn_next"
        app:layout_constraintTop_toBottomOf="@id/setup_progress">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Name Input -->
            <TextView
                style="@style/SetupLabelStyle"
                android:text="@string/your_name" />

            <EditText
                style="@style/SetupInputStyle"
                android:autofillHints="name"
                android:hint="@string/enter_your_name"
                android:inputType="textPersonName" />

            <!-- Currency Select -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/select_currency" />

            <LinearLayout
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:gravity="center_vertical">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/indian_rupee"
                    android:textColor="@color/white"
                    android:textSize="16sp" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="Select"
                    android:src="@android:drawable/arrow_down_float"
                    app:tint="@color/white" />
            </LinearLayout>

            <!-- Pocket Money Input -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/monthly_pocket_money" />

            <RelativeLayout
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp">

                <EditText
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:autofillHints="none"
                    android:background="@android:color/transparent"
                    android:hint="5000"
                    android:inputType="number"
                    android:textColor="@color/white"
                    android:textColorHint="@color/text_muted" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:contentDescription="Stepper"
                    android:src="@android:drawable/btn_dialog"
                    app:tint="@color/white" />
            </RelativeLayout>

            <!-- Date Input -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/pocket_money_date" />

            <LinearLayout
                android:id="@+id/date_pocket_money_container"
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_pocket_money_date"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/date_hint"
                    android:textColor="@color/text_muted" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="Calendar"
                    android:src="@android:drawable/ic_menu_my_calendar"
                    app:tint="@color/text_muted" />
            </LinearLayout>

            <!-- College Name -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/college_name" />

            <EditText
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:autofillHints="organizationName"
                android:hint="@string/enter_college_name"
                android:inputType="text" />

            <!-- Semester Fee -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/semester_fee_amount" />

            <EditText
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:autofillHints="none"
                android:hint="45000"
                android:inputType="number" />

            <!-- Due Date -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="20dp"
                android:text="@string/next_semester_due_date" />

            <LinearLayout
                android:id="@+id/date_due_date_container"
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="24dp"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tv_due_date"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="@string/date_hint"
                    android:textColor="@color/text_muted" />

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:contentDescription="Calendar"
                    android:src="@android:drawable/ic_menu_my_calendar"
                    app:tint="@color/text_muted" />
            </LinearLayout>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_next"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_margin="16dp"
        android:backgroundTint="@color/accent_lime"
        android:text="@string/next_btn"
        android:textAllCaps="false"
        android:textColor="@color/bg_dark"
        android:textSize="18sp"
        android:textStyle="bold"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>