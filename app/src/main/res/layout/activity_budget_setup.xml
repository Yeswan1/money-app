<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark">

    <!-- Header Section -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btn_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="Back"
            android:src="@android:drawable/ic_menu_revert"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/white" />

        <TextView
            android:id="@+id/header_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="serif"
            android:text="@string/budget_setup"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/step_2_2"
            android:textColor="@color/text_muted"
            android:textSize="12sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/setup_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginHorizontal="16dp"
        android:progress="100"
        android:progressDrawable="@drawable/custom_progress_bar"
        app:layout_constraintTop_toBottomOf="@id/header" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintBottom_toTopOf="@id/btn_finish"
        app:layout_constraintTop_toBottomOf="@id/setup_progress">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/budget_setup_subtitle"
                android:textColor="@color/text_muted"
                android:textSize="14sp"
                android:layout_marginBottom="24dp"/>

            <!-- Total Limit -->
            <TextView
                style="@style/SetupLabelStyle"
                android:text="@string/total_monthly_limit" />

            <EditText
                android:id="@+id/et_total_limit"
                style="@style/SetupInputStyle"
                android:hint="20000"
                android:inputType="numberDecimal" />

            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="32dp"
                android:text="@string/category_budgets" />

            <!-- Food Budget -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="16dp"
                android:text="@string/food_budget"
                android:textSize="10sp" />

            <EditText
                android:id="@+id/et_food_budget"
                style="@style/SetupInputStyle"
                android:hint="5000"
                android:inputType="numberDecimal" />

            <!-- Shopping Budget -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="16dp"
                android:text="@string/shopping_budget"
                android:textSize="10sp" />

            <EditText
                android:id="@+id/et_shopping_budget"
                style="@style/SetupInputStyle"
                android:hint="3000"
                android:inputType="numberDecimal" />

            <!-- Transport Budget -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="16dp"
                android:text="@string/transport_budget"
                android:textSize="10sp" />

            <EditText
                android:id="@+id/et_transport_budget"
                style="@style/SetupInputStyle"
                android:hint="2000"
                android:inputType="numberDecimal" />

            <!-- Entertainment Budget -->
            <TextView
                style="@style/SetupLabelStyle"
                android:layout_marginTop="16dp"
                android:text="@string/entertainment_budget"
                android:textSize="10sp" />

            <EditText
                android:id="@+id/et_entertainment_budget"
                style="@style/SetupInputStyle"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="24dp"
                android:hint="2000"
                android:inputType="numberDecimal" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_finish"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_margin="16dp"
        android:backgroundTint="@color/accent_lime"
        android:text="@string/complete_setup"
        android:textAllCaps="false"
        android:textColor="@color/bg_dark"
        android:textSize="18sp"
        android:textStyle="bold"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>